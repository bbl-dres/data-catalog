# Style token consistency review

Reviewed 2026-09-05. Scope: the active prototype's `css/tokens.css`, `css/main.css`, `css/graph.css`, header/home markup and styles generated by JavaScript. Archived wireframes, third-party Swagger styles and SVG assets are outside this cleanup.

## Findings and changes

| Finding | Implementation |
|---|---|
| Collection tiles used a literal 12 px gap. | `.ob-tiles` now uses `--ob-tile-gap`, which resolves to `--ob-space-default` (16 px). Every group retains the same grid and column width rules. |
| Repeated gaps, padding, font sizes, line heights, weights, border widths and radii bypassed existing tokens. | Main and diagram components now consume the shared scale. The compact app additions are explicitly labelled in `tokens.css`: 6 px control spacing, 15 px reading text, 16 px touch-input text and 18 px label line height. Existing values are preserved except the intentional changes listed below. |
| Component tokens described old dimensions but did not control the components. | Connected the avatar (28 px), drawer (320 px), drawer header (56 px), tile minimum height (74 px), facts labels (160/128 px) and phone table labels (96 px) to their actual styles. Removed obsolete tree range, badge, KPI minimum, tabs minimum and graph halo tokens. |
| Reusable layout dimensions were scattered through `main.css`. | Added named sidebar, rail, flyout, hero and home-content dimensions. `--ob-home-max-width` controls both the KPI grid and vertically stacked home tables at 1120 px. The standalone API width derives from its content width and gutters. |
| `compactTables` set a custom property that table cells never used. | Normal tables now use 8 px vertical padding and a 36 px minimum cell height; compact tables use 4 px and 28 px. Single-line rows measure 37/29 px including the border. Facts retain their own padding/height tokens, so opening additional metadata keeps the normal 37 px row rhythm. Touch targets remain at least 44 px. |
| Logo/header/facts responsive token overrides were mixed into component CSS. | Centralised those values in `tokens.css`; component display/layout rules remain in `main.css`. Corrected stale font-size comments and documented where the compact scale differs from Oblique. |
| A white avatar label and diagram shadow bypassed semantic styles. | Uses `--ob-color-text-inverse` and the existing `--ob-shadow-sm`; diagram count badges use the shared pill radius. |
| JavaScript duplicated visual spacing and constraints. | Handbook scrolling reads the computed CSS scroll margin. Diagram height supplies viewport geometry while CSS supplies its minimum and bottom gap. Hero suggestions pass available viewport height to CSS, which owns the maximum, touch minimum and bottom clearance. The load-error message uses a class instead of inline margin styling. |

## Visible refinements

- Tile gaps increase from 12 to 16 px.
- Hero description spacing follows the 16 px scale; the next section starts after the standard 24 px gap on all widths.
- The header has a flag followed by a 16 px gap and the organisation/app text. The wordmark and divider are omitted at the user's request; their unused styles/tokens are removed. Flag breakpoints remain intact. Title and subtitle text cap at 16 px from 1280 px onwards, keeping the same compact typography on very large screens instead of growing to 18 px at 1920 px.
- The example searches under the hero form are removed, including their styles and unused translation key. The input, submit button, suggestions and header shortcut continue to work.
- Normal typography and row density remain the same. Enabling the existing compact-table option now reduces table row padding as intended.

## Values intentionally kept explicit

Do not turn every number into a token. Use a token for a shared design decision or configurable component dimension, rather than merely moving a literal elsewhere.

- **Native media/container breakpoints:** CSS custom properties cannot be used as query conditions. Viewport queries govern header/navigation; container queries govern content layouts.
- **Diagram geometry:** the restored bubble diagram supplies node, halo, caption, grid and pager dimensions from `graph.layout()` to CSS as custom properties. Change these with the algorithm so actual circles and calculated bounds stay aligned. Decorative spacing, controls, type, borders and colours use shared tokens. The original halo colour token is used again by the central bubble.
- **Data-driven inline values:** tree depth, table column proportions, diagram coordinates/transforms and measured viewport space belong to the data/layout layer.
- **Structural and optical details:** zero values, fractional widths, circular radii, visually hidden 1 px boxes, border overlaps, icon baseline offsets, compact chip/phone-field padding and flag aspect-ratio dimensions retain their meaning without a generic spacing alias.
- **Interaction heuristics:** zoom bounds, gesture thresholds and the short-screen search scroll trigger are behaviour, not visual style tokens.

## Maintenance rule

Use semantic colour tokens; use existing spacing, type, weight, radius and border tokens directly. Introduce a component token when it gives a meaningful adjustment point, such as `--ob-tile-gap` or `--ob-home-max-width`. Keep responsive value overrides beside the base token. Preserve distinct input targets and data-row density instead of equating every coincidentally equal measurement.

The property audit reduced literal spacing/type/radius declarations from 172 to 8 in `main.css` and from 45 to 2 in `graph.css`. These counts exclude dimensions, colours and borders; they measure repeated style literals, not general code quality. All `--ob-*` references in the active CSS/JS resolve to a definition or a runtime value.

## Verification

- Core checks: 12 passing tests.
- Existing functional checks: home/header search, keyboard interaction, menus, exports, metadata and API lifecycle pass.
- Responsive checks: 150 layouts and 3,904 profile combinations pass, including phone/tablet input, short-screen search, consistent metadata heights and print.
- Diagram checks: navigation, selection, paging, fullscreen, touch gestures and a 1,000-relationship layout pass.
- A temporary browser inspection checks 16 px tile gaps and equal tile widths, the shared home width, the simplified header/search at six widths (320–3840 px), and the actual compact configuration response. No app data is changed by these checks.

Commands and environment setup are in [tests/README.md](../tests/README.md).
