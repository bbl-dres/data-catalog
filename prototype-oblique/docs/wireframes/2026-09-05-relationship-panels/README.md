# Relationship panel reference

Saved before restoring the relationship bubble diagram on 2026-09-05. These two files preserve the panel layout and its viewport interactions for possible future data-lineage design work.

This is a component reference, not a standalone prototype. `graph.js` depends on the active prototype's `DK.ui`, `DK.data` and application event delegation; `graph.css` depends on its tokens and icon assets. The current relationships still describe associations, so these connectors do not represent data flow or lineage semantics.
