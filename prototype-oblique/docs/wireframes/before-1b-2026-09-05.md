# Prototype before option 1b

[Open the preserved prototype](before-1b-2026-09-05/index.html).

Created on 2026-09-05 before implementing **1b Seitenspalte – bevorzugt** in the root `prototype-oblique` application. The copy includes all runtime files and supporting documentation: 70 files verified against their source hashes. The enclosing wireframes directory is excluded to avoid recursive copies.

Serve the repository over HTTP, just like the root prototype. All runtime assets and data in this snapshot use relative paths and are self-contained.
